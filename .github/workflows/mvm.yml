name: mvm 

on:
  [push]

permissions: write-all

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        shell: bash
    steps:
      - uses: actions/checkout@v4
      - name: Tests
        run: bash tests.sh
      - name: Create staging directory for Graphs and CSVs
        run: mkdir staging
      - name: Copy graphs
        run: cp -r graphs staging
      - name: Copy CSVs
        run: cp combined_test_results.csv staging & cp MVM/mvm_test_results.csv staging & cp MVM_CKPT/ckpt_test_results.csv staging & cp SIMPLE_CKPT/simple_ckpt_test_results.csv staging
      - uses: actions/upload-artifact@v4
        with:
          name: Package
          path: staging